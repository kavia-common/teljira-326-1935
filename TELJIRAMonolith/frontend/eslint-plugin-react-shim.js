export default {
  rules: {
    // Minimal no-op rules to satisfy ESLint plugin interface if the real plugin is missing
  },
  configs: {},
  meta: {
    name: 'eslint-plugin-react-shim',
    version: '0.0.0'
  }
};
