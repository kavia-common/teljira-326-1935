# Integrations Layer related variables
# Shared secret for inbound webhooks signature validation (HMAC SHA-256, header x-sf-signature = sha256=<hex>)
WEBHOOK_SECRET=change-me

# Allow unauthenticated inbound chat/webhook route (defaults to false/not set)
INTEGRATIONS_INBOUND_ALLOW_ANON=false

# Example provider tokens (do not commit real secrets)
GITHUB_TOKEN=
GITLAB_TOKEN=
BITBUCKET_TOKEN=
AZURE_DEVOPS_TOKEN=
