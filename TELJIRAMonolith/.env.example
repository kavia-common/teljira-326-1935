# Server
PORT=3001
HOST=0.0.0.0
NODE_ENV=development

# Database (PostgreSQL)
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=sprintfLow
PG_USER=postgres
PG_PASSWORD=postgres
PG_SSL=false

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=1d
PASSWORD_SALT_ROUNDS=12

# MFA (TOTP)
MFA_ISSUER=SprintFlow
MFA_ENABLED=true

# CORS
CORS_ORIGIN=http://localhost:5173

# App
SITE_URL=http://localhost:5173

# Encryption (optional at-rest field encryption)
FIELD_CRYPTO_KEY=replace_with_32_bytes_key

# Webhooks/Integrations
WEBHOOK_OUTBOUND_ENABLED=true

# Email/Notifications (stubs - replace with real providers)
NOTIFY_PROVIDER=log
