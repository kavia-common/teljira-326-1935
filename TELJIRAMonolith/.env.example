# TELJIRAMonolith (SprintFlow) - .env example
# Copy this file to .env and customize values for your environment.
# Do NOT commit the .env file.

# Server
PORT=3000
HOST=0.0.0.0
SITE_URL=http://localhost:3000
NODE_ENV=development

# PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=sprintflow
PGUSER=sf_user
PGPASSWORD=sf_password
# For managed PG that requires SSL, set to "require". Otherwise "disable".
PGSSLMODE=disable

# JWT / Sessions
JWT_SECRET=replace-with-strong-secret
JWT_EXPIRES_IN=1h
COOKIE_SECRET=replace-with-cookie-secret
PASSWORD_PEPPER=optional-static-pepper

# Security
# Name of the CSRF cookie. Browser clients without Authorization will receive this cookie automatically.
CSRF_COOKIE_NAME=csrf_token
WEBHOOK_SECRET=replace-with-strong-hmac-secret

# CORS Allowed Frontend Origin (optional; add in addition to local dev origin)
# SITE_URL can be used by CORS allowlist automatically; for multiple origins, configure at runtime if needed.
# Example: SITE_URL=https://your-deployment.example.com

# Socket.IO
# No configuration required by default; will inherit CORS origins from above.

# Notes:
# - Ensure the database exists and credentials are valid before running migrations.
# - After setting up .env, run:
#     npm install
#     npm run migrate
#     npm run seed
#     npm run dev
#   Then, start the frontend:
#     npm --prefix frontend run dev
